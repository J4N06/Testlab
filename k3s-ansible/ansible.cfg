[defaults]
inventory          = inventory/hosts.yml
host_key_checking  = False
remote_user        = ubuntu
private_key_file   = ~/.ssh/id_rsa   # SSH-Key anpassen
stdout_callback    = yaml
callbacks_enabled  = timer, profile_tasks

[privilege_escalation]
become      = True
become_method = sudo
